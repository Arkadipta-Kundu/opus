package org.arkadipta.opus.entity;

public enum TaskStatus {
    TODO, // Will be stored as 0 in database
    IN_PROGRESS, // Will be stored as 1 in database
    DONE // Will be stored as 2 in database
}
